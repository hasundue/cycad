{
  "tasks": {
    "run": "deno run --allow-env --allow-read --allow-write=. --allow-run --allow-net=deno.land,registry.npmjs.org,api.github.com",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "cache": "deno cache ./**/*.ts --lock",
    "lock": "deno task cache --lock-write",
    "build": "deno task run src/langs.ts",
    "test": "deno test --parallel",
    "dev": "deno task fmt && deno task lint && deno task lock && deno task build && deno task test",
    "update": "deno task -q run https://deno.land/x/molt@0.5.0/cli.ts check ./**/*.ts"
  },
  "fmt": {
    "exclude": [
      "CHANGELOG.md"
    ]
  },
  "lint": {
    "rules": {
      "include": [
        "no-sync-fn-in-async-fn"
      ]
    }
  }
}
